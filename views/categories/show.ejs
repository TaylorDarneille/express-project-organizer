<h1><%= category.name %></h1>

<h2>Projects</h2>

<% if(category && category.projects) { %>
    <% category.projects.forEach(function(p) { %>
        <div class="well">
            <h3><%= p.name %></h3>
        </div>
    <% }) %>
<% } else { %>
    <h3>No projects yet!</h3>
<% } %>
